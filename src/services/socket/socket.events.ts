/**
 * Mirror of `shared/enums#SocketEvent`. Re-exported here so socket
 * consumers import from `@services/socket` and we can later evolve the
 * frontend's event vocabulary independently if needed.
 */
export { SocketEvent } from '@shared/enums';
